var React = require('react');
var ReactDOM = require('react-dom');\

/*this will let us make <StorePicker/> */

var StorePicker = React.createClass({
    render: function() {
        return (
      <div>
        )
    }
});

ReactDOM.render(<StorePicker/>, document.querySelector.getElementById("#main"));
